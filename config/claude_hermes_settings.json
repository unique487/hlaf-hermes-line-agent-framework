{
  "$comment": "Passed to `claude -p ... --settings config/claude_hermes_settings.json` by app/services/claude_code_agent.py. Registers scripts/claude_hermes_hook.py as a PreToolUse hook for every tool call — the script decides approve/block itself (see app/services/tools.is_dangerous), so the matcher intentionally covers all tools rather than a subset. UNVERIFIED ASSUMPTION: this hooks JSON shape (hooks.PreToolUse[].matcher/hooks[].type=command/command/timeout) and the $CLAUDE_PROJECT_DIR variable follow the documented Claude Code settings.json hooks convention as of this writing; cross-check against the installed CLI version's hooks docs before relying on it in production.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "python \"$CLAUDE_PROJECT_DIR/scripts/claude_hermes_hook.py\"",
            "timeout": 630
          }
        ]
      }
    ]
  }
}
