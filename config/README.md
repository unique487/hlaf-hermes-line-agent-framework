# config/

This directory holds non-Python runtime configuration files (e.g. YAML/JSON/TOML)
that will be introduced as the application grows. It is separate from
`app/config.py`, which holds environment-driven `pydantic-settings` configuration.

Currently empty — no configuration files exist yet.
